# Database Configuration - using Railway environment variables
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:music_app}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:123456}

# Keep update to ensure schema remains consistent
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Port configuration - Railway uses PORT environment variable
server.port=${PORT:8080}

# Cloudinary Configuration
cloudinary.cloud_name=dnd4apm6t
cloudinary.api_key=736711585279733
cloudinary.api_secret=AteGeHbsB_9HN3DJ5PzCxr2RUv8

# Jackson Naming Strategy
spring.jackson.property-naming-strategy=SNAKE_CASE
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.fail-on-empty-beans=false
spring.jpa.open-in-view=true

# Multipart File Upload Limits
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Encryption Secret for Playlist Sharing
app.share.secret=${SHARE_SECRET:AppAdMusic2026SecureKey!}
